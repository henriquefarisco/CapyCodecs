# CapyCodecs

Version: 0.0.1

CapyCodecs owns portable image codec contracts for CapyOS services.

## Validation

```sh
make validate
```

The release gate compiles with strict C warnings, runs image contract tests, checks release metadata and verifies hardened compile flags.
